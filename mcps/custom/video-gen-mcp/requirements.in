mcp>=1.28.1,<2.0
httpx>=0.27,<1.0
# Bundled static ffmpeg binary (frame extraction for transitions) — no system
# ffmpeg on any execution target; wheels exist for linux/mac/win satellites.
imageio-ffmpeg>=0.6,<1.0
